/**
 * prisma.config.ts — intentionally left minimal.
 * Prisma v5 does not use this file for configuration.
 * All Prisma settings are in prisma/schema.prisma.
 * DATABASE_URL is loaded from the root .env file.
 */
export {};
